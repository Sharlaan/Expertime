module.exports = {
  transpileDependencies: ['vuetify'],
  // Redefine the proxy port used by webpack devServer, so it does not get blocked by CORS (env dev only)
  // devServer: {
  //   port: 8080,
  // },
};
